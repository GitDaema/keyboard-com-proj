from __future__ import annotations

# Placeholder for model-specific helpers if needed later (e.g., remapping)
# For now, direct HID backend uses label->index map provided by JSON and
# maintains its own cache, so nothing is required here.